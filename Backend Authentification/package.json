{
  "name": "backend-authentification",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",  // Start script for Render
    "build": "echo 'No build step required'" // Optional build step; can be customized if needed
  },
  "author": "yasmin seba",
  "license": "ISC",
  "description": "A backend service for user authentication and management.",
  "dependencies": {
    "axios": "^1.7.7",
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.3",
    "ejs": "^3.1.10",
    "express": "^4.21.0",
    "express-session": "^1.18.0",
    "mariadb": "^3.3.2",
    "mysql2": "^3.11.3"
  },
  "engines": {
    "node": ">=14.x" // Specify the Node.js version you want to use
  }
}
